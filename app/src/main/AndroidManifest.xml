<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.poly.hailt.bookmanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">
        <activity
            android:name=".ui.SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppThemeNoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".ui.UserActivity" />
        <activity android:name=".ui.ChangePasswordActivity" />
        <activity android:name=".ui.AddUserActivity" />
        <activity android:name=".ui.CategoryActivity" />
        <activity android:name=".ui.AddCategoryActivity" />
        <activity android:name=".ui.BookActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.AddBookActivity" />
        <activity android:name=".ui.BillActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.AddBillActivity" />
        <activity android:name=".ui.AddBillDetailActivity" />
        <activity android:name=".ui.BestSellerActivity" />
        <activity android:name=".ui.AnalyticActivity" />
        <activity android:name=".ui.UserDetailActivity" />
        <activity android:name=".ui.CategoryDetailActivity" />
        <activity android:name=".ui.BookDetailActivity" />
        <activity android:name=".ui.BillDetailActivity" />
        <activity android:name=".ui.BookOfCategoryActivity" />
    </application>

</manifest>